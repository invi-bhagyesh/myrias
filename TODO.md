# Next steps

- Connect the private Hugging Face dataset only after explicit upload authorization.
- Add authenticated framework execution and server-side secret handling.
- Add full PDF extraction with page locators and source-version tracking.
- Extend graph provenance and confidence calibration with the adapted IAA schema.
- Add shared review, concurrent-edit reconciliation, and full revision backup/restore.
- Improve large-graph navigation and typeset report rendering beyond browser printing.

## Pending verification — requested by user

- Check clustered graph layout, cluster boundaries, scroll zoom, Shift-scroll pan, dragging, click-to-open records, hover/selected connection glow, and keyboard focus.
- Check dark/light mode persistence, contrast, desktop/mobile layout, and existing research/profile flows.
- This interaction/theme update was implemented without running tests or browser checks, as requested. Run these checks when the user asks for the next verification round.
